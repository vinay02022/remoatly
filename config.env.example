# MongoDB Configuration
MONGODB_URI=mongodb+srv://vinay_123:vinay_123@project.exuhvf5.mongodb.net/real-time?retryWrites=true&w=majority&appName=project
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/leaderboard_db

# Server Configuration
PORT=3000
NODE_ENV=development

# Application Settings
MAX_LEADERBOARD_SIZE=1000
CACHE_TTL_SECONDS=60
DEFAULT_REGION=global

# Security (for production)
JWT_SECRET=your_jwt_secret_here_change_in_production
CORS_ORIGIN=http://localhost:3000

# Performance Settings
DB_CONNECTION_POOL_SIZE=10
SOCKET_PING_TIMEOUT=5000
SOCKET_PING_INTERVAL=25000

# Rate Limiting (requests per minute)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info 